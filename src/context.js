import React from 'react';

// create context global state
const userContext = React.createContext(null);
const setRequestContext = React.createContext(null);

export { userContext , setRequestContext};